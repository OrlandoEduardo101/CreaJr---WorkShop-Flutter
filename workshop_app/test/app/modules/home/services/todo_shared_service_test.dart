import 'package:flutter_test/flutter_test.dart';

import 'package:workshop_app/app/modules/home/services/todo_shared_service.dart';

void main() {
  TodoSharedService service;

  setUp(() {
//    service = TodoSharedService();
  });
//
  group('TodoSharedService Test', () {
//    test("First Test", () {
//      expect(service, isInstanceOf<TodoSharedService>());
//    });
//
  });
}
