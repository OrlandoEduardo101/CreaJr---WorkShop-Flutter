import 'package:flutter_test/flutter_test.dart';

import 'package:app_workshop/app/modules/home/services/localStorage/local_storage_shared_service.dart';

void main() {
  LocalStorageSharedService service;

  setUp(() {
//    service = LocalStorageSharedService();
  });
//
  group('LocalStorageSharedService Test', () {
//    test("First Test", () {
//      expect(service, isInstanceOf<LocalStorageSharedService>());
//    });
//
  });
}
