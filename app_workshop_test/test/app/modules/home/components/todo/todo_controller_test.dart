import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:app_workshop/app/modules/home/components/todo/todo_controller.dart';
import 'package:app_workshop/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // TodoController todo;
  //
  setUp(() {
    //     todo = HomeModule.to.get<TodoController>();
  });

  group('TodoController Test', () {
    //   test("First Test", () {
    //     expect(todo, isInstanceOf<TodoController>());
    //   });

    //   test("Set Value", () {
    //     expect(todo.value, equals(0));
    //     todo.increment();
    //     expect(todo.value, equals(1));
    //   });
  });
}
